# To Do List

These items will be part of the design.

- 	the appeals workflow
- 	the company fairness dashboard UI (fairness metrics, audit logs)
-	the bias correction algorithm
-	the candidate portal UI
- 	the candidate fairness dashboard UI
-	the data model for ghosting events
- 	the data model for fairness metrics
-	the data model (tables/collections for candidates, jobs, matches, traces, companies)
-	the explanation schema
-	design the formal JSON schema
	-	design the Job Brief schema
	-	define the bias correction layer
	-	define the explanation format
-	design the portal architecture (candidate onboarding, company dashboard)
-	design the reference implementation structure (folders, modules, services)
-	the ghosting SLA rules (governance-level)
- 	the governance escalation pipeline
-	the governance dashboard UI
-	the identity model (candidate IDs, company IDs, match IDs)
-	the matching engine pseudocode/internals
-	the multi model inference layer
-	the nightly fairness computation job
-	the scoring formula
- 	the SLA rules (governance-level)
-	the trace schema
